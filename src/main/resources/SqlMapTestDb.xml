<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" 
	"HTTP://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>

	<!-- These settings control SqlMapClient configuration details, primarily to do with transaction
	management. They are all optional (more detail later in this document). -->

	<settings>
		<setting name=cacheModelsEnabled value="true" />
		<setting name=enhancementEnabled value="true" />
		<setting name=lazyLoadingEnabled value="true" />
		<setting name=maxRequests value="16" />
		<setting name=maxSessions value="3" />
		<setting name=maxTransactions value="3" />
		<setting name=useStatementNamespaces value="false" />
		<setting name=defaultStatementTimeout value="5000" />
		<setting name=statementCachingEnabled value="true" />
		<setting name=classInfoCacheEnabled value="true" />
	</settings>

	<mappers>
		<mapper resource="sqlMap/testDbSql.xml" />
	</mappers>

</configuration>